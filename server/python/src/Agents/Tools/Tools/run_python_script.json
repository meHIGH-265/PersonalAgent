{
  "name": "run_python_script",
  "description": "Runs a python script and returns its output.\nfile_path: the relative path of the python file.\nreturns: the output of the python script (which may include errors) or a message indicating that the file did not run.",
  "args_schema": {
    "file_path": "str"
  }
}